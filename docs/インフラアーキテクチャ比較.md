# インフラアーキテクチャ比較設計書

> **作成日:** 2026-03-04
> **更新日:** 2026-03-04（AWS Lightsail を追加・推奨を更新）
> **目的:** Phase 1 着手前のインフラ技術選定の最終確認。Google + Supabase を基準軸に、OTP2 デプロイを主要変数として各アーキテクチャを比較する。

---

## 1. 前提・設計上のポイント

### 1.1 コンポーネント構成

本プロジェクトで必要なインフラコンポーネントは以下の通り。

| コンポーネント | 役割 |
|---|---|
| **FastAPI バックエンド** | REST API サーバー（認証・スケジュール・提案等） |
| **OTP2 サーバー** | 経路探索エンジン（Java, GraphQL API）|
| **PostgreSQL DB** | ユーザー・スケジュールデータ永続化 |
| **フロントエンド CDN** | SPA ホスティング |
| **FCM** | プッシュ通知 |

### 1.2 OTP2 が選定の最大変数

FastAPI・DB・フロントエンドはいずれも無料枠内に収まるが、**OTP2 のデプロイ先が月次コストの大半を占める**。

OTP2 の特性上、デプロイ先には以下の制約がある。

| 要件 | 理由 |
|---|---|
| **常時起動（スリープ不可）** | graph.obj (709 MB) をメモリに展開しており、コールドスタートに2〜5分かかる |
| **RAM 2〜4 GB 必須** | graph.obj 709 MB + JVM ヒープ + ルーティング演算バッファ |
| **ステートフル（永続化）** | graph.obj のディスク保持が必要 |
| **GraphQL on 8080** | FastAPI が `OTP2_GRAPHQL_URL` 環境変数で参照 |

このため、Cloud Run（ゼロスケール型）や Railway（コスト高）は OTP2 に適さない。

---

## 2. OTP2 デプロイ選択肢の個別比較

### 2.1 GCE e2-medium（推奨 A）

| 項目 | 内容 |
|---|---|
| スペック | 2 vCPU（共有）/ 4 GB RAM |
| 常時起動 | ✅（VM は停止しない限り継続稼働） |
| RAM 十分性 | ✅（4 GB は OTP2 に十分。余裕あり） |
| ストレージ | 20 GB SSD ディスク $4.42/月 |
| 月額コスト | **$31.38 + $4.42 ≒ $36/月**（Tokyo: asia-northeast1） |
| セットアップ難度 | 中（SSH ログイン → systemd でサービス化） |
| OTP2起動方法 | systemd unit で `java -jar otp.jar --serve data/` を自動起動 |
| リージョン | asia-northeast1（東京） |

**メリット**
- RAM 余裕あり → OOM リスクなし
- GCP 統合（Cloud Logging・Cloud Monitoring で FastAPI と同じ画面で監視可）
- VM 再起動ポリシーで crash 時の自動復旧設定が容易
- GCP のマネージド環境で SLA が比較的高い

**デメリット**
- 3 サービス（Cloud Run + GCE + Supabase）の管理が必要
- GCE の SSH セットアップ・ファイアウォール設定が必要
- FastAPI と OTP2 が別 VPC 扱いになるため VPC 内通信不可（外部 IP で通信）

---

### 2.2 fly.io shared-cpu-2x 4GB（推奨 B）

| 項目 | 内容 |
|---|---|
| スペック | 2 vCPU（共有）/ 4 GB RAM |
| 常時起動 | ✅（Machine を起動したままにする設定が可） |
| RAM 十分性 | ✅（4 GB で余裕あり） |
| ストレージ | persistent volume 2 GB $0.30/月 |
| 月額コスト | **$22.22（iad 基準）+ 東京 nrt プレミアム約 30% ≒ $29/月** + volume $0.30/月 ≒ **$30/月** |
| セットアップ難度 | 低〜中（`flyctl` + Dockerfile + `fly.toml` のみ） |
| OTP2 起動方法 | Dockerfile で OTP2 JAR を COPY → volume マウントで graph.obj 参照 |
| リージョン | nrt（東京） |

**メリット**
- `flyctl deploy` 1コマンドでコンテナデプロイ完了
- Dockerfile ベースで CI/CD 対応しやすい
- 東京リージョン（nrt）対応でレイテンシ低い
- GCE より VM 管理が少ない（SSH 不要、コンテナ abstraction）
- persistent volume でグラフファイルを保持（Docker イメージを軽量に保てる）

**デメリット**
- fly.io と GCP の 2 サービス管理が必要
- shared CPU なので OTP2 の経路計算が集中すると GCE より遅い可能性がある
- fly.io のサポートは GCP より手薄
- 東京プレミアム込みのコストは GCE と大差なくなる（$29 vs $31）

**OTP2 graph.obj の扱い（fly.io 版）**

```
Option 1: Docker image に graph.obj を COPY で焼き込む
  - メリット: シンプル、volume 不要
  - デメリット: イメージサイズ ~1 GB（プッシュに時間がかかる）
  - GTFS 更新時は全リビルド必要

Option 2: persistent volume に graph.obj を置く（推奨）
  - イメージは OTP2 JAR (~200 MB) のみ
  - 初回のみ volume に graph.obj をコピー（flyctl sftp / ssh コマンド）
  - GTFS 更新時はボリュームの graph.obj を差し替えるだけ
```

---

### 2.3 fly.io shared-cpu-1x 2GB（最安型・リスクあり）

| 項目 | 内容 |
|---|---|
| スペック | 1 vCPU（共有）/ 2 GB RAM |
| RAM 十分性 | △（graph 709 MB + JVM overhead + routing ≒ 1.8〜2.0 GB でギリギリ） |
| 月額コスト | **$11.11（iad 基準）+ 東京プレミアム ≒ $14/月** + volume $0.30/月 ≒ **$15/月** |

**メリット**
- ハッカソン期間中なら最安オプション

**デメリット**
- OOM（Out of Memory）のリスクがある
- シングル vCPU は OTP2 の経路計算で遅延する可能性あり
- ハッカソンのデモ直前にクラッシュするリスクを許容できるか？

---

### 2.4 AWS Lightsail（最安・東京対応）

| 項目 | 内容 |
|---|---|
| スペック（推奨プラン） | 2 vCPU / 4 GB RAM / 80 GB SSD |
| 常時起動 | ✅ |
| RAM 十分性 | ✅（4 GB で余裕あり） |
| ストレージ | 80 GB SSD **込み**（別途ディスク費用なし） |
| 月額コスト | **$24/月**（Tokyo: ap-northeast-1）|
| セットアップ難度 | 中（SSH → systemd、GCE と同等） |
| OTP2 起動方法 | systemd unit で `java -jar otp.jar --serve data/` を自動起動 |
| リージョン | ap-northeast-1（東京） |

**メリット**
- **4 つの選択肢の中で最安（$24/月）** —— GCE ($38) より $14 安く、fly.io 4GB ($30) より $6 安い
- ディスク代込みの定額なので**費用が予測しやすい**
- 東京リージョン（ap-northeast-1）対応
- EC2 ベースで安定性が高い（AWS SLA 99.5%）
- 4TB/月のデータ転送込み（ハッカソン規模では十分）

**デメリット**
- GCP（Cloud Run）との**クロスクラウド**になる（管理コンソールが分散する）
- AWS と GCP の 2 アカウント管理が必要
- Lightsail は独立したサービスで、他の AWS サービス（Lambda 等）との VPC 統合がしにくい
- fly.io の `flyctl deploy` より手順がやや多い（EC2 SSH 操作が必要）

**2GB 版（$12/月）との比較**

| | $12/月（2GB） | $24/月（4GB）|
|---|---|---|
| RAM | 2 GB（OOM リスクあり） | 4 GB（余裕） |
| SSD | 60 GB | 80 GB |
| 推奨 | ハッカソン期間限定・リスク許容時のみ | 本番デモには必須 |

---

### 2.5 GCE Spot VM e2-medium（格安・不安定）

| 項目 | 内容 |
|---|---|
| 月額コスト | e2-medium Tokyo 通常 $31.38 の約 56% OFF ≒ **$14/月** |
| リスク | GCP に随時終了される可能性あり（30秒通知のみ） |
| OTP2 影響 | 終了後の再起動で graph 読み込み（1〜2 分の停止）が発生 |

**適用場面**: デモ頻度が低い開発期間中。デモ当日・直前は通常 VM に切り替え推奨。

---

### 2.5 GCP Cloud Run（OTP2 用途では非推奨）

| 項目 | 内容 |
|---|---|
| min-instances=1 + 1 vCPU + 2 GB RAM | ~$57/月（always-on CPU: $0.000018/vCPU-秒） |
| min-instances=1 + 2 vCPU + 4 GB RAM | ~$104/月 |
| コールドスタート | 2〜5 分（graph.obj 709 MB の JVM 読み込み） |

**結論: Cloud Run は OTP2 に不適。** ゼロスケールと相性が悪く、min=1 を設定すると GCE より高額になる。

---

### 2.6 Railway Hobby（OTP2 用途では非推奨）

| 項目 | 内容 |
|---|---|
| OTP2 目安（1 vCPU + 2 GB, 常時起動） | CPU $20.02 + RAM $20.02 - $5 クレジット = **約 $40/月** |
| OTP2 目安（1 vCPU + 4 GB, 常時起動） | CPU $20 + RAM $40 - $5 クレジット = **約 $55/月** |

**結論: Railway は常時起動のメモリ集約型ワークロードに不向き。** 料金体系が割高になる。

---

### 2.8 OTP2 ホスト選択肢まとめ

| 選択肢 | 月額（東京） | RAM | 安定性 | 難易度 | 推奨度 |
|---|---|---|---|---|---|
| **AWS Lightsail $24** | **$24** | 4 GB ✅ | ★★★★★ | 中 | ★★★★★ |
| fly.io 2vCPU 4GB | ~$30 | 4 GB ✅ | ★★★★☆ | 低 | ★★★★☆ |
| GCE e2-medium | ~$38 | 4 GB ✅ | ★★★★★ | 中 | ★★★★☆ |
| AWS Lightsail $12 | $12 | 2 GB △ | ★★★★☆ | 中 | ★★★☆☆ |
| fly.io 1vCPU 2GB | ~$15 | 2 GB △ | ★★★☆☆ | 低 | ★★★☆☆ |
| GCE e2-medium Spot | ~$14 | 4 GB ✅ | ★★☆☆☆ | 中 | ★★☆☆☆ |
| Cloud Run min=1 | ~$57 | 2 GB △ | ★★★☆☆ | 低 | ★☆☆☆☆ |
| Railway Hobby | ~$40 | 2 GB △ | ★★★☆☆ | 低 | ★☆☆☆☆ |

> **なぜ Lightsail が最安か？**
> GCE は VM 本体 + ディスク代が分離課金（$31 + $4 = $35〜）。fly.io は Tokyo 地域プレミアム込みで $28〜30。
> Lightsail は 4GB RAM + 80GB SSD + 4TB 転送が **$24 定額**なため割安になる。

---

## 3. フルアーキテクチャ案

### Plan A: GCP + GCE OTP2 + Supabase（GCP安定型）

```
┌─────────────┐    ┌─────────────────────────────────────────┐
│   Vercel    │    │           Google Cloud Platform          │
│  (Frontend) │    │                                          │
│  SPA 無料   │    │  ┌─────────────┐   ┌─────────────────┐  │
└─────┬───────┘    │  │  Cloud Run  │   │  Compute Engine │  │
      │            │  │  FastAPI    │──▶│  OTP2 (e2-med)  │  │
      │ HTTP       │  │  (無料枠)   │   │  4GB RAM        │  │
      └──────────▶ │  └──────┬──────┘   │  tokyo         │  │
                   │         │           └─────────────────┘  │
                   │         │ SQL                             │
                   └─────────┼─────────────────────────────────┘
                             │
                   ┌─────────▼──────────┐   ┌──────────────┐
                   │     Supabase       │   │     FCM      │
                   │  PostgreSQL 無料枠  │   │  プッシュ通知  │
                   └────────────────────┘   └──────────────┘
```

| コンポーネント | サービス | 月額 |
|---|---|---|
| FastAPI | GCP Cloud Run | $0（無料枠内） |
| OTP2 | GCE e2-medium, asia-northeast1 | $31.38 |
| OTP2 ディスク | pd-ssd 20 GB | $4.42 |
| フロントエンド | Vercel Hobby | $0 |
| DB | Supabase Free | $0 |
| プッシュ通知 | FCM | $0 |
| **合計** | | **~$36/月** |

**向いている場面**
- GCP を一元管理したい
- Cloud Logging・Cloud Monitoring で FastAPI と OTP2 をまとめて監視したい
- OTP2 の安定性・RAM 余裕を最優先にしたい

**非機能要件対応**

| 観点 | 評価 |
|---|---|
| 可用性 | GCE は GCP SLA 99.5%。Cloud Run は 99.95%。Supabase 無料は SLA なし |
| レイテンシ | FastAPI・OTP2 ともに東京リージョン。API 往復 < 100ms 期待値 |
| スケーラビリティ | FastAPI は Cloud Run で自動スケール。OTP2 は固定 1 VM（ハッカソンでは問題なし） |
| セキュリティ | GCE はファイアウォールで 8080 を Cloud Run からのみ許可。Supabase は TLS 接続のみ |
| 監視・ロギング | Cloud Logging + Cloud Monitoring で統合管理可 |
| バックアップ | Supabase 無料は日次バックアップなし（手動エクスポート必要） |
| コスト予測精度 | GCE は固定料金で予測しやすい |

---

### Plan B: GCP + fly.io OTP2 + Supabase（コスト最適型）

```
┌─────────────┐    ┌───────────────────┐    ┌───────────────────┐
│   Vercel    │    │  Google Cloud     │    │     fly.io        │
│  (Frontend) │    │                   │    │                   │
│  SPA 無料   │    │  ┌─────────────┐  │    │  ┌─────────────┐  │
└─────┬───────┘    │  │  Cloud Run  │──┼───▶│  │    OTP2     │  │
      │            │  │  FastAPI    │  │    │  │ 2vCPU 4GB   │  │
      │ HTTP       │  │  (無料枠)   │  │    │  │ nrt(Tokyo)  │  │
      └──────────▶ │  └──────┬──────┘  │    │  └─────────────┘  │
                   │         │          │    └───────────────────┘
                   └─────────┼──────────┘
                             │ SQL
                   ┌─────────▼──────────┐   ┌──────────────┐
                   │     Supabase       │   │     FCM      │
                   │  PostgreSQL 無料枠  │   │  プッシュ通知  │
                   └────────────────────┘   └──────────────┘
```

| コンポーネント | サービス | 月額 |
|---|---|---|
| FastAPI | GCP Cloud Run | $0（無料枠内） |
| OTP2 | fly.io shared-cpu-2x 4GB, nrt | ~$29 |
| OTP2 ボリューム | fly.io persistent volume 2 GB | $0.30 |
| フロントエンド | Vercel Hobby | $0 |
| DB | Supabase Free | $0 |
| プッシュ通知 | FCM | $0 |
| **合計** | | **~$30/月** |

**向いている場面**
- コストを Plan A より抑えたい（$36 → $30）
- OTP2 を Dockerfile + flyctl でシンプルにデプロイしたい
- VM の SSH 管理を避けたい

**非機能要件対応**

| 観点 | 評価 |
|---|---|
| 可用性 | fly.io は SLA 明記なし。ただし実績的に安定（個人開発向け） |
| レイテンシ | fly.io nrt は東京。API 往復 < 150ms 期待値 |
| スケーラビリティ | OTP2 は固定 1 Machine（ハッカソンでは問題なし） |
| セキュリティ | fly.io は内部ネットワーク + Wireguard で保護。公開ポート設定が必要 |
| 監視・ロギング | fly.io は `flyctl logs` で確認可。GCP Cloud Logging との統合は別途設定必要 |
| コスト予測精度 | fly.io は時間課金（常時起動なら固定額に近い） |

---

### Plan C: GCP + fly.io OTP2 2GB + Supabase（最安型）

Plan B と同構成で OTP2 を `shared-cpu-1x 2GB` に縮小。

| コンポーネント | サービス | 月額 |
|---|---|---|
| FastAPI | GCP Cloud Run | $0（無料枠内） |
| OTP2 | fly.io shared-cpu-1x 2GB, nrt | ~$14 |
| OTP2 ボリューム | fly.io persistent volume 2 GB | $0.30 |
| フロントエンド | Vercel Hobby | $0 |
| DB | Supabase Free | $0 |
| プッシュ通知 | FCM | $0 |
| **合計** | | **~$15/月** |

> ⚠️ **注意:** 2GB RAM はギリギリのため OOM リスクあり。ハッカソン本番での使用は推奨しない。

---

### Plan D: GCP Cloud Run + AWS Lightsail OTP2 + Supabase（最安・安定型）

```
┌─────────────┐    ┌───────────────────┐    ┌───────────────────┐
│   Vercel    │    │  Google Cloud     │    │       AWS         │
│  (Frontend) │    │                   │    │                   │
│  SPA 無料   │    │  ┌─────────────┐  │    │  ┌─────────────┐  │
└─────┬───────┘    │  │  Cloud Run  │──┼───▶│  │  Lightsail  │  │
      │            │  │  FastAPI    │  │    │  │    OTP2     │  │
      │ HTTP       │  │  (無料枠)   │  │    │  │ 2vCPU 4GB   │  │
      └──────────▶ │  └──────┬──────┘  │    │  │  ap-ne-1    │  │
                   │         │          │    │  │  (Tokyo)    │  │
                   └─────────┼──────────┘    │  └─────────────┘  │
                             │ SQL           └───────────────────┘
                   ┌─────────▼──────────┐   ┌──────────────┐
                   │     Supabase       │   │     FCM      │
                   │  PostgreSQL 無料枠  │   │  プッシュ通知  │
                   └────────────────────┘   └──────────────┘
```

| コンポーネント | サービス | 月額 |
|---|---|---|
| FastAPI | GCP Cloud Run | $0（無料枠内） |
| OTP2 | AWS Lightsail 4GB, ap-northeast-1 | **$24** |
| OTP2 ストレージ | Lightsail 80GB SSD込み | $0（別途不要） |
| フロントエンド | Vercel Hobby | $0 |
| DB | Supabase Free | $0 |
| プッシュ通知 | FCM | $0 |
| **合計** | | **~$24/月** |

**向いている場面**
- コストを最小化しつつ 4GB RAM の安定性を確保したい
- AWS に多少慣れている（または慣れる意欲がある）
- GCP/AWS の 2 クラウド管理を許容できる

**非機能要件対応**

| 観点 | 評価 |
|---|---|
| 可用性 | AWS Lightsail は EC2 ベース。SLA 99.5% |
| レイテンシ | Lightsail ap-northeast-1（東京）。Cloud Run Tokyo → Lightsail Tokyo で往復 < 20ms |
| セキュリティ | Lightsail ファイアウォールで 8080 ポートを Cloud Run の外部 IP のみに制限 |
| 監視・ロギング | Lightsail は CloudWatch 統合あり。GCP とは別ダッシュボード |
| セットアップ工数 | 中（SSH + systemd。GCE と同等の手順） |
| コスト予測精度 | 完全定額で最もわかりやすい |

---

## 4. コスト見積もり詳細

### 4.1 月額コスト比較表（全プラン）

| | Plan A (GCE) | Plan B (fly.io 4GB) | Plan C (fly.io 2GB) | **Plan D (Lightsail)** |
|---|---|---|---|---|
| FastAPI (Cloud Run) | $0 | $0 | $0 | $0 |
| OTP2 ホスト | $31.38 | ~$29 | ~$14 | **$24** |
| OTP2 ストレージ | $4.42 (20GB SSD) | $0.30 (2GB vol) | $0.30 (2GB vol) | **$0**（込み） |
| DB (Supabase Free) | $0 | $0 | $0 | $0 |
| フロントエンド (Vercel) | $0 | $0 | $0 | $0 |
| FCM | $0 | $0 | $0 | $0 |
| **月額合計** | **~$36** | **~$30** | **~$15** | **~$24** |

### 4.2 Cloud Run コスト試算（FastAPI）

ハッカソン規模（月 10,000 リクエスト程度）では無料枠の範囲内に完全に収まる。

| リソース | 無料枠 | 試算使用量 | 超過額 |
|---|---|---|---|
| リクエスト数 | 2,000,000/月 | ~10,000 | $0 |
| vCPU 秒 | 180,000/月 | ~2,000（200ms × 10,000req） | $0 |
| GB 秒 | 360,000/月 | ~2,000（512MB × 4,000秒） | $0 |

**FastAPI の Cloud Run コスト: $0/月**

### 4.3 OTP2 コスト試算（GCE e2-medium, asia-northeast1）

| 項目 | 計算 | 金額 |
|---|---|---|
| VM 本体 | e2-medium 東京 = $31.38/月 | $31.38 |
| ブートディスク | pd-ssd 20 GB × $0.221/GB = $4.42/月 | $4.42 |
| 静的外部 IP | $0.004/時間 × 720 = $2.88/月（割り当て済み） | $2.88 |
| **合計** | | **~$38.68/月** |

> 静的 IP は Cloud Run → GCE の通信に使用。動的 IP でも動作するが DNS 設定が変わる。

### 4.4 OTP2 コスト試算（AWS Lightsail 4GB, ap-northeast-1）

| 項目 | 計算 | 金額 |
|---|---|---|
| Lightsail $24 プラン | 2vCPU / 4GB RAM / 80GB SSD / 4TB 転送 込み定額 | $24.00 |
| 追加ディスク | 不要（80GB SSD 込み） | $0 |
| 静的 IP | Lightsail の固定 IP は 1 つ無料（アタッチ中） | $0 |
| **合計** | | **$24.00/月** |

> **GCE（$38.68）との差額: $14.68/月 ≒ 年間 $176 節約**
> **fly.io 4GB（$29.30）との差額: $5.30/月 ≒ 年間 $64 節約**

### 4.5 OTP2 コスト試算（fly.io shared-cpu-2x 4GB, nrt）

| 項目 | 計算 | 金額 |
|---|---|---|
| Machine 本体 | $22.22（iad ベース）× 1.3（nrt プレミアム） | ~$28.88 |
| Persistent volume | 2 GB × $0.15/GB | $0.30 |
| **合計** | | **~$29.18/月** |

### 4.5 外部 API コスト試算

#### WeatherAPI.com
- 無料枠: **1,000,000 API コール/月**
- ハッカソン試算: ユーザー数 100 × 1 日 5 回 × 30 日 = 15,000 コール/月
- **コスト: $0**（無料枠の 1.5%）

#### Gemini API（AI 提案）
- 無料枠: Gemini 2.0 Flash = 15 RPM / 1,500 RPD（1日1,500リクエストまで）
- 試算: 1 リクエストあたり ~1,000 input tokens + ~500 output tokens
- ハッカソン試算: 100 ユーザー × 2 回/日 × 30 日 = 6,000 リクエスト/月
- 無料枠（1,500 RPD = 45,000/月）で余裕でカバー
- **コスト: $0**（無料枠内）
- 万一有料になった場合: 6,000 リクエスト × (1,000 input × $0.10/1M + 500 output × $0.40/1M) ≒ **$1.80/月**

#### Supabase（万一 Pro Plan が必要になった場合）
- 現在の無料枠: 500 MB DB ストレージ・7 日間無活動でポーズ
- Pro Plan: **$25/月**（ポーズなし・8 GB ストレージ）
- ハッカソン期間中は無料枠で十分（スキーマ + 少量データ）
- **本番運用継続なら Pro Plan への移行を検討**

---

## 5. 非機能要件 総合比較

| 観点 | Plan A (GCE) | Plan B (fly.io 4GB) | Plan C (fly.io 2GB) | **Plan D (Lightsail)** |
|---|---|---|---|---|
| **可用性** | GCE SLA 99.5% | SLA 明記なし | SLA 明記なし | **AWS SLA 99.5%** |
| **OTP2 安定性** | ★★★★★（4GB） | ★★★★☆（4GB） | ★★☆☆☆（OOM） | **★★★★★（4GB）** |
| **レイテンシ** | 東京 ✅ | 東京 ✅ | 東京 ✅ | **東京 ✅** |
| **スケール** | 手動 | 手動 | 手動 | 手動 |
| **セキュリティ** | GCP FW | fly.io WG | fly.io WG | **AWS FW** |
| **監視** | Cloud Monitoring統合 | flyctl logs | flyctl logs | CloudWatch（別画面） |
| **セットアップ工数** | 中（SSH + systemd） | 低（flyctl） | 低（flyctl） | 中（SSH + systemd） |
| **クラウド統合** | GCP単一 ✅ | GCP+fly.io | GCP+fly.io | **GCP+AWS（分散）** |
| **コスト** | ~$36/月 | ~$30/月 | ~$15/月 | **~$24/月** |
| **デモ安定度** | ★★★★★ | ★★★★☆ | ★★☆☆☆ | **★★★★★** |

---

## 6. Supabase 無料枠の注意点

ハッカソン開発中に Supabase 無料プロジェクトが **7 日間無活動でポーズ** される点に注意が必要。

| タイミング | 対策 |
|---|---|
| 開発期間中 | 週 1 回以上 API リクエストを飛ばすか、定期的にダッシュボードにアクセスして手動再開する |
| ハッカソン当日 | 前日に必ずダッシュボードでアクティブ状態を確認する |
| 長期運用する場合 | Supabase Pro ($25/月) へアップグレードするか、Railway PostgreSQL / Neon に移行する |

---

## 7. 推奨・結論

### コスト順位（4GB RAM・東京リージョン）

| 順位 | Plan | OTP2 | 月額 | 備考 |
|---|---|---|---|---|
| 🥇 | **Plan D** | AWS Lightsail $24 | **$24/月** | コスト最安・安定性高・ただしクロスクラウド |
| 🥈 | Plan B | fly.io 4GB | ~$30/月 | セットアップ最も簡単 |
| 🥉 | Plan A | GCE e2-medium | ~$36/月 | GCP 統合管理 |

### 第 1 推奨: **Plan D（AWS Lightsail OTP2 + GCP Cloud Run + Supabase）**

| 理由 | 詳細 |
|---|---|
| **全案中で最安（$24/月）** | GCE より $12 安く、fly.io より $6 安い。年間で $144〜$176 の節約 |
| **4GB RAM で安定** | OOM リスクなし。GCE・fly.io 4GB と同等の安定性 |
| **ディスク込み定額** | 80GB SSD 込みの固定料金。追加課金なし |
| **東京リージョン** | ap-northeast-1（東京）でレイテンシ低い |
| **AWS SLA 99.5%** | GCE と同等の信頼性 |

> 🔀 **クロスクラウドのトレードオフ:** Cloud Run（GCP）と Lightsail（AWS）を使うため管理コンソールが分散する。GCP 一本化にこだわりがある場合は Plan A。

### 第 2 推奨: **Plan B（fly.io 4GB OTP2 + GCP Cloud Run + Supabase）**

コスト $30/月で Plan D より $6 高いが、`flyctl deploy` で SSH 不要のコンテナデプロイが可能。**セットアップ速度を最優先にするなら Plan B。**

### 第 3 推奨: **Plan A（GCE e2-medium + GCP Cloud Run + Supabase）**

GCP で統一管理（Cloud Logging・Cloud Monitoring 統合）したい場合に選択。コストは $36/月。

### 非推奨
- Cloud Run での OTP2 ホスト（コールドスタート問題・コスト過大: ~$57/月）
- Railway での OTP2 ホスト（RAM コスト過大: ~$40/月）
- fly.io 2GB OTP2（OOM リスク）

---

## 8. 意思決定チェックリスト（更新版）

- [ ] **OTP2 デプロイ先を確定する**
  - 推奨: Plan D（AWS Lightsail $24 Tokyo）> Plan B（fly.io 4GB nrt）> Plan A（GCE e2-medium Tokyo）
  - `backend/docs/経路探索API設計調査.md § T-2` のブロッカーが解消する
- [ ] **フロントエンドホスティングを確定する**
  - 推奨: Vercel（無料・最速デプロイ）
- [ ] **DB を確定する**
  - 推奨: Supabase Free（週1以上活動で無料継続可）
- [ ] **プッシュ通知を確定する**
  - 推奨: FCM（無料・iOS/Android 両対応）

---

## 9. 料金ソース一覧

| サービス | URL |
|---|---|
| GCE インスタンス価格（東京） | https://gcloud-compute.com/e2-medium.html |
| GCE ディスク価格 | https://gcloud-compute.com/diskpricing.html |
| GCP 公式 VM 料金ページ | https://cloud.google.com/compute/vm-instance-pricing |
| GCP Spot VM 割引率 | https://cloud.google.com/spot-vms/pricing |
| Cloud Run 公式料金ページ | https://cloud.google.com/run/pricing |
| Cloud Run 料金解説 | https://cloudchipr.com/blog/cloud-run-pricing |
| Cloud Run always-on 試算 | https://hamy.xyz/blog/2025-04_google-cloud-run-pricing |
| fly.io 料金ドキュメント | https://fly.io/docs/about/pricing/ |
| Railway 料金ページ | https://railway.com/pricing |
| Railway プランドキュメント | https://docs.railway.com/reference/pricing/plans |
| Supabase 料金ページ | https://supabase.com/pricing |
| Supabase 料金解説 | https://uibakery.io/blog/supabase-pricing |
| Vercel Hobby プランドキュメント | https://vercel.com/docs/plans/hobby |
| Firebase Hosting 料金 | https://firebase.google.com/docs/hosting/usage-quotas-pricing |
| WeatherAPI.com 料金ページ | https://www.weatherapi.com/pricing.aspx |
| Gemini API 料金ページ | https://ai.google.dev/pricing |
| Gemini API レート制限 | https://ai.google.dev/gemini-api/docs/rate-limits |
| economize.cloud GCE e2-medium | https://www.economize.cloud/resources/gcp/pricing/compute-engine/e2-medium/ |
| economize.cloud GCE e2-small | https://www.economize.cloud/resources/gcp/pricing/compute-engine/e2-small/ |
| AWS Lightsail 公式料金ページ | https://aws.amazon.com/lightsail/pricing/ |
| AWS Lightsail インスタンスバンドル | https://docs.aws.amazon.com/lightsail/latest/userguide/amazon-lightsail-bundles.html |
| AWS Lightsail 料金解説 | https://kuberns.com/blogs/post/aws-lightsail-pricing-your-comprehensive-guide/ |

---

## 参考ドキュメント（相互リンク）

- `docs/インフラ設計調査.md` — 各サービスの概要比較（前身ドキュメント）
- `backend/docs/経路探索API設計調査.md` — OTP2 デプロイ要件詳細・T-2 ブロッカー
- `backend/docs/開発タスク.md` — Phase 1 着手前の意思決定チェックリスト
