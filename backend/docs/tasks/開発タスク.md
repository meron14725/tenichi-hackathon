# バックエンド 開発スケジュール

> **担当:** バックエンド 1人（Python / FastAPI / PostgreSQL）
> **進め方:** フェーズ順に進む。Phase 0 の意思決定は完了済み。Phase 1（インフラ）と Phase 2（基盤構築）を並行して進める。

---

## 凡例

| マーク | 優先度 | 意味 |
|--------|--------|------|
| 🔴 | HIGH | ブロッカー / 実装開始に必須 |
| 🟡 | MEDIUM | コア機能、早めに着手 |
| 🟢 | LOW | ブロッカー待ち or ハッカソンスコープ外候補 |

---

## Phase 0: 意思決定

> 実装ブロッカーになる未決定事項を先に潰す。
> 根拠は `backend/docs/spec/データモデル.md § 仕様確認が必要な事項` を参照。

### 認証 / ユーザー

- [x] 🔴 **E-1** 認証方式を確定する（JWT / OAuth2）— **JWT のみ**（アクセストークン + リフレッシュトークン）
- [x] 🔴 **D-1** `UserSettings` / `NotificationSettings` の初期作成タイミングを確定する（register 時一括 / 遅延作成）— **register 時一括作成**
- [x] 🟢 **E-2** パスワードリセット機能をハッカソンスコープに含めるか決める — **スコープ外**

### タグ

- [x] 🔴 **A-1** タグのスコープを確定する（グローバル全ユーザー共通 / per-user）— **グローバル**（`Tag.name UNIQUE` 維持）
- [x] 🔴 **A-2** タグ作成権限を確定する（ユーザーが自由作成 / 事前定義のみ）— **事前定義のみ**（seed 投入、`POST /tags` は実装しない）

### Schedule / Template

- [x] 🔴 **B-1** `travel_mode` ENUM 値を確定する（walking / cycling / transit / driving で十分か）— **4 値で確定**
- [x] 🟡 **B-3** `Schedule.travel_mode` を nullable にするか確定する（目的地なし予定への対応）— **nullable**
- [x] 🟡 **B-4** 終日フラグ（`is_all_day`）の要否を確定する（カレンダーUI連携）— **不要**
- [x] 🟡 **B-5** ソフトデリート（`deleted_at`）の要否を確定する — **不要（物理削除）**
- [x] 🟢 **B-6** 繰り返し予定の対応範囲を決める（ハッカソンスコープ外候補）— **現時点は非対応。`recurrence_rule VARCHAR(500)` nullable を追加**

### 通知

- [x] 🟢 **C-1** リマインダー複数設定の対応範囲を決める（現在は `reminder_minutes_before` 1つ）— **現時点は 1 つのみ**

### 外部API

- [x] 🟡 **Routes** 経路 API を選定する（Google Directions API / Yahoo! 乗換 API 等）— **OTP2**（確定。GTFS・グラフビルド済み。）
- [x] 🟢 **Suggestions** 提案ロジックのアプローチを決定する — **Gemini API を使って実装（確定）**
- [x] 🟢 **Infra-OTP2** OTP2 デプロイ先を確定する — **GCP Cloud Run マルチサービス構成（案A）確定**
      `infra/docs/` に設計書・構築手順書・CI/CDパイプライン設計を追加済み

---

## Phase 1: インフラ構築（GCP セットアップ）

> 詳細手順: `infra/docs/インフラ構築手順書.md`
> Phase 2（backend 実装）と並行して進める。初回デプロイに必要な最小セット（§1〜8）を優先。
> 1人体制なら順番に、複数人なら並行実施可能。

### GCP初期セットアップ（初回のみ）

- [ ] 🔴 GCPプロジェクト作成・請求先リンク・デフォルトリージョン設定（§1）
- [ ] 🔴 必要なAPI有効化（Cloud Run / Cloud Build / Artifact Registry / Secret Manager 等）（§2）
- [ ] 🔴 サービスアカウント作成（fastapi-sa, otp2-sa）+ Cloud Build SAへの権限付与（§3）
- [ ] 🔴 Artifact Registry Docker リポジトリ作成（§4）
- [ ] 🔴 Secret Manager にシークレット作成（6個）・fastapi-sa にアクセス権付与（§5）

### デプロイ（backend実装後）

- [ ] 🔴 FastAPI Backend の初回デプロイ（§6）
- [ ] 🔴 OTP2 Server の初回デプロイ（§7）
- [ ] 🔴 OTP2 内部URL を Secret Manager に設定 + fastapi-sa に invoker 権限付与（§8）
- [ ] 🟡 Cloud Build CI/CDトリガー作成（GitHub接続 + backend/otp2トリガー）（§9）

### その他（ハッカソン後半）

- [ ] 🟡 Firebase プロジェクト追加 + iOS APNs設定（§11）
- [ ] 🟡 モニタリング・アラート設定（OTP2メモリ80%超アラート等）（§12）
- [ ] 🟢 予算アラート設定（$60/月）（§13）

---

## Phase 2: 基盤構築

> FastAPI / DB / マイグレーション / 共通ミドルウェアのセットアップ

- [x] 🔴 FastAPI プロジェクトのディレクトリ構成を決めて作成する
- [x] 🔴 依存パッケージを定義する（`pyproject.toml` / `requirements.txt`）
      追加必須: `google-auth`, `google-auth-httplib2`（OTP2 IAM認証用）
- [x] 🔴 環境変数管理を実装する（`.env` / `pydantic-settings`）
- [x] 🔴 PostgreSQL 接続設定を実装する（SQLAlchemy async + asyncpg）
- [x] 🔴 Alembic マイグレーションの初期設定をする
- [x] 🔴 Dockerfile を作成する（FastAPI Backend 用 — Cloud Run デプロイに必須）
      参照: `infra/docs/CI-CDパイプライン設計.md § 3`（cloudbuild-backend.yaml）
      ポート 8000、依存関係レイヤーキャッシュ最適化
- [x] 🟡 docker-compose を作成する（ローカル開発用: PostgreSQL + FastAPI）
- [x] 🟡 共通エラーレスポンス形式を実装する（`backend/docs/spec/API詳細設計.md § エラーレスポンス` 準拠）
- [x] 🟡 ヘルスチェックエンドポイントを実装する（`GET /healthz`）
- [x] 🟢 CORS ミドルウェアを設定する（iOS本番は不要。ローカル開発時のみ検討）
      参照: `infra/docs/ネットワーク・セキュリティ設計.md § 4`

---

## Phase 3: 認証 (Auth)

> Phase 0 の E-1（認証方式）確定後に着手

- [x] 🔴 `User` モデルを作成しマイグレーションを実行する
- [x] 🔴 パスワードハッシュを実装する（bcrypt）
- [x] 🔴 JWT アクセストークン生成・検証ロジックを実装する
- [x] 🔴 認証依存注入を実装する（`get_current_user`）
- [x] 🔴 `POST /auth/register` を実装する
- [x] 🔴 `POST /auth/login` を実装する
- [x] 🟡 リフレッシュトークン管理を実装する
- [x] 🟡 `POST /auth/refresh` を実装する
- [x] 🟡 `POST /auth/logout` を実装する

---

## Phase 4: ユーザー (Users)

> Phase 0 の D-1（UserSettings 初期作成タイミング）確定後に着手

- [x] 🔴 `UserSettings` モデルを作成しマイグレーションを実行する
- [x] 🔴 `GET /users/me` を実装する
- [x] 🔴 `PUT /users/me` を実装する
- [x] 🔴 `GET /users/me/settings` を実装する
- [x] 🔴 `PUT /users/me/settings` を実装する

---

## Phase 5: コアAPI (Tags / Schedules / Templates)

> Phase 0 の A-1・A-2・B-1（タグスコープ/作成権限、travel_mode ENUM）確定後に着手

### Tags

- [x] 🔴 `Tag` モデルを作成しマイグレーションを実行する
- [x] 🔴 `GET /tags` を実装する（タグ一覧取得）
- [ ] 🟡 `POST /tags` を実装する（タグ作成 — A-2 の決定に依存）— **事前定義のみのためスコープ外**

### Schedules

- [x] 🔴 `Schedule` / `ScheduleTag` モデルを作成しマイグレーションを実行する
- [x] 🔴 `GET /schedules` を実装する（クエリパラメータ: `start_date` / `end_date`）
- [x] 🔴 `POST /schedules` を実装する
- [x] 🔴 `GET /schedules/{id}` を実装する
- [x] 🔴 `PUT /schedules/{id}` を実装する
- [x] 🔴 `DELETE /schedules/{id}` を実装する

### Templates

> Template は「1日の予定集合の雛形」。`Template` / `TemplateCategory` / `TemplateSchedule` / `TemplateScheduleTag` を実装する。

- [x] 🟡 `Template` / `TemplateCategory` / `TemplateSchedule` / `TemplateScheduleTag` モデルを作成しマイグレーションを実行する
- [x] 🟡 `GET /templates` を実装する（TemplateSchedule・TemplateCategory をネストして返す）
- [x] 🟡 `POST /templates` を実装する（schedules 配列を一括作成）
- [x] 🟡 `GET /templates/{id}` を実装する
- [x] 🟡 `PUT /templates/{id}` を実装する
- [x] 🟡 `DELETE /templates/{id}` を実装する
- [x] 🟡 `POST /templates/{id}/apply` を実装する（`date` を受け取り TemplateSchedule の時刻と組み合わせて Schedule を複数作成、ScheduleList も自動生成）

### ScheduleLists（予定リスト）

> 予定リストは「1日の予定をまとめた名前付きリスト」。カレンダー表示用。Template（再利用雛形）とは別概念。`ScheduleList` / `PackingItem` を実装する。

- [x] 🟡 `ScheduleList` / `PackingItem` モデルを作成しマイグレーションを実行する
- [x] 🟡 `Schedule` モデルに `schedule_list_id` カラムを追加するマイグレーションを実行する
- [x] 🟡 `GET /schedule-lists` を実装する（クエリパラメータ: `start_date` / `end_date`）
- [x] 🟡 `POST /schedule-lists` を実装する（packing_items 配列を一括作成）
- [x] 🟡 `GET /schedule-lists/{id}` を実装する（所属予定 + 持ち物をネストして返す）
- [x] 🟡 `PUT /schedule-lists/{id}` を実装する
- [x] 🟡 `DELETE /schedule-lists/{id}` を実装する（持ち物カスケード削除、Schedule の `schedule_list_id` を null に）
- [x] 🟡 `POST /schedule-lists/{id}/packing-items` を実装する
- [x] 🟡 `PUT /schedule-lists/{id}/packing-items/{item_id}` を実装する
- [x] 🟡 `DELETE /schedule-lists/{id}/packing-items/{item_id}` を実装する

---

## Phase 6: 外部API連携 — 天気 (Weather)

> WeatherAPI.com を使用（`backend/docs/spec/API詳細設計.md § Weather` 参照）

- [x] 🟡 WeatherAPI.com クライアントを実装する
- [x] 🟡 API キーを環境変数で管理する
- [x] 🟡 `GET /weather` を実装する（指定日時・場所の天気取得）
- [x] 🟡 `GET /weather/forecast` を実装する（3日間予報取得）

---

## Phase 7: 通知設定 (Notifications)

> Phase 0 の D-1（NotificationSettings 初期作成タイミング）確定後に着手

- [x] 🟡 `NotificationSettings` モデルを作成しマイグレーションを実行する
- [x] 🟡 `DeviceToken` モデルを作成しマイグレーションを実行する
- [x] 🟡 `GET /notifications/settings` を実装する
- [x] 🟡 `PUT /notifications/settings` を実装する
- [x] 🟡 `POST /notifications/tokens` を実装する（デバイストークン登録）
- [x] 🟡 `DELETE /notifications/tokens/{token}` を実装する

---

## Phase 8: 経路API (Routes)

> OTP2 on Cloud Run（案A）確定済み。インフラ構築（Phase 1 §7〜8）後に着手可能。
> 詳細: `backend/docs/spec/経路探索API設計.md`

- [x] 🟡 OTP2 GraphQL クライアントを実装する（HTTPx + Google ID Token 認証）
      実装パターン: `infra/docs/ネットワーク・セキュリティ設計.md § 2.2`
      `google.oauth2.id_token.fetch_id_token()` で `OTP2_GRAPHQL_URL` を audience に指定
- [x] 🟡 `POST /routes/departure-time` を実装する（OTP2 planConnection クエリ）
      クエリ設計: `backend/docs/spec/経路探索API設計.md § 4.1`
- [x] 🟡 `POST /routes/search` を実装する（経路検索・複数ルート候補返却）

### ScheduleRoute（選択ルート保持）

> 予定作成時にモバイルアプリが OTP2 で取得したルート候補からユーザーが選択した1つを予定に紐づけて保持する。
> 詳細: `backend/docs/spec/経路探索API設計.md § 8.7`

- [x] 🟡 `ScheduleRoute` モデルを作成しマイグレーションを実行する
- [x] 🟡 `POST /schedules/{id}/route` を実装する（選択ルート保存・置換）
- [x] 🟡 `GET /schedules/{id}/route` を実装する
- [x] 🟡 `DELETE /schedules/{id}/route` を実装する

---

## Phase 9: 提案 (Suggestions)

> **ブロッカー解消済み。** Gemini API を使った AI 提案として設計確定。
> 詳細は `backend/docs/spec/API詳細設計.md § Suggestions` 参照

- [x] 🟡 Gemini API クライアントを実装する
- [x] 🟡 API キーを環境変数で管理する
- [x] 🟡 `GET /suggestions/today` を実装する（今日のスケジュール + 天気情報を Gemini API に渡して服装・持ち物の提案を生成）
- [x] 🟡 `GET /suggestions/{schedule_id}` を実装する（指定 Schedule の目的地・タグ・メモ情報を Gemini API に渡して周辺スポット・アドバイスを生成）

---

## Phase 10: テスト・品質

> Phase 5 完了後から並行して進めることを推奨

- [x] 🟡 pytest セットアップをする（テスト用 DB 設定含む）
- [x] 🟡 リンター / フォーマッターを設定する（ruff / black）
- [x] 🟡 Auth API のテストを書く
- [x] 🟡 Users API のテストを書く
- [x] 🟡 Schedules API のテストを書く
- [x] 🟡 Tags / Templates API のテストを書く
- [x] 🟢 Weather API のテストを書く（外部 API モックを使用）
- [ ] 🟢 テストカバレッジを計測する（目標: コアAPI 80% 以上）
- [x] 🟢 CI（GitHub Actions）を設定する（lint + test の自動実行）

---

## 結合テスト: OTP2 + FastAPI + DB (docker-compose)

> Phase 8 完了後、Phase 9 に進む前に OTP2 との結合テストを実施。

- [x] 🟡 OTP2 Dockerfile の JAR 取得先を修正する（Maven Central → GitHub Releases、v2.7.0）
- [x] 🟡 docker-compose の OTP2 ヘルスチェックを修正する（`/otp/actuators/health` → `/otp/`、start_period 増加）
- [x] 🟡 docker compose up で 3 サービス（FastAPI + PostgreSQL + OTP2）の起動を確認する
- [x] 🟡 Alembic マイグレーションを docker 環境で実行・確認する
- [x] 🟡 OTP2 GraphQL の `dateTime: "now"` エラーを修正する（`otp2_client.py`）
- [x] 🟡 Routes API (`POST /routes/search`) の E2E 動作を確認する（東京→渋谷の経路取得成功）
- [x] 🟡 Departure Time API (`POST /routes/departure-time`) の E2E 動作を確認する

---

## 全体の着手優先順（クリティカルパス）

```
Week 1（今すぐ）
  ├── Infra: GCPプロジェクト・API・SA・AR・Secrets（Phase 1 §1〜5）
  └── Backend: Phase 2（基盤構築）+ Dockerfile

Week 1-2
  Backend: Phase 3（Auth）→ Phase 4（Users）→ Phase 5（Tags / Schedules）
  Infra: FastAPI 初回デプロイ（Phase 1 §6）

Week 2
  Backend: Phase 6（Weather）→ Phase 7（Notifications）→ Phase 8（Routes）
  Infra: OTP2 デプロイ → CI/CD トリガー（Phase 1 §7〜9）

Week 3
  Backend: Phase 9（Suggestions）
  Infra: Firebase + APNs（Phase 1 §11）
  Backend: Phase 10（テスト・品質）並行開始

デモ直前
  エンドツーエンドテスト（infra/docs/インフラ構築手順書.md 付録D チェックリスト）
```
